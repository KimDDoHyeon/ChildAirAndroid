<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.childair">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="kidsair.kidsscrip.childair.ChildBoard" />
        <activity android:name="kidsair.kidsscrip.childair.Dust" />
        <activity android:name="kidsair.kidsscrip.childair.FoodMenu" />
        <activity android:name="kidsair.kidsscrip.childair.Result" />
        <activity android:name="kidsair.kidsscrip.childair.Calendar" />
        <activity android:name="kidsair.kidsscrip.childair.MainActivity">

        </activity>
        <activity android:name="kidsair.kidsscrip.childair.SpalshScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>